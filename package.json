{
  "name": "quota-router-agy",
  "version": "1.1.0",
  "type": "module",
  "description": "Claude Code plugin to delegate research tasks to agy",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18"
  }
}
